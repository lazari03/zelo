// Simple localization utility (placeholder)
// In production, replace with i18next or next-intl
export function t(key: string, fallback: string) {
  // TODO: Load translations from JSON or context
  return fallback;
}
